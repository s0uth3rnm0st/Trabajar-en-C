#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "archivos.h"
/*
int loadAlumno(Alumno* pPerson)
{
    int ret=1;
    getString("Ingrese nombre ",pPerson->name);

    pPerson->age = getInt("Ingrese la edad ");
    if(pPerson->age<=0 || pPerson->age>100)
    {
        ret=0;
    }

    return ret;
}
*/
